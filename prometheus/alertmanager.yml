global: {}

route:
  receiver: local-alerts
  group_by:
    - alertname
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: local-alerts
    webhook_configs:
      - url: http://127.0.0.1:5001/alerts
        send_resolved: true
